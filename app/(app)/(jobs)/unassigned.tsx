import { SafeAreaView, View, StyleSheet } from 'react-native';

import { Text } from '~/components/nativewindui/Text';

export const Unassigned = () => {
  return (
    <SafeAreaView>
      <Text>Unassigned Jobs</Text>
    </SafeAreaView>
  );
};

export default Unassigned;
